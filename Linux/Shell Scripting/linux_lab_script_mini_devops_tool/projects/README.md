# Projects Folder

Create your Mini DevOps Tool here as instructed in the clues.

- **ops_tool.sh** – Single script: case menu (check, status, backup, pipeline, exit), functions (check_services with for loop, show_status with date/uptime/df/free, backup_now, run_pipeline), exit 0/1.
